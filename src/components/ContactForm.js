import React from "react";

const ContactForm = () => {
  return (
    <section id="contact">
      <h2>Contact Me</h2>
      <form>{/*  fields for name, email, message, etc. */}</form>
    </section>
  );
};

export default ContactForm;
